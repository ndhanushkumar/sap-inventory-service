spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local

server:
  port: 7073

---
spring:
  application:
    name: SAP-INVENTORY-SERVICE
# eureka:
#   client:
#     register-with-eureka: true
#     fetch-registry: true
#     service-url:
#       defaultZone: http://localhost:8761/eureka/
#   instance:
#     hostname: localhost

management:
  tracing:
    sampling:
      probability: 1.0